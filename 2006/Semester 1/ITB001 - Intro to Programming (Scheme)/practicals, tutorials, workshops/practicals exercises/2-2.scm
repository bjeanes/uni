;;   (2 + 3)              ;; procedure should come first ... i.e.: (+ 2 3)
;;   (+ 1 2 3))           ;; unexpected ')' ... should be (+ 1 2 3)
;;   ((+ 3 4))            ;; extra parentheses expect an extra procedure
;;   (+ (* 2 3) (> 2 3))  ;; cannot add a boolean to an integer

(display "\ncommented out procedures to run one at a time :-)\n")